from django.contrib import admin

"""
This file is used to register models for the Django admin interface.

By registering models here, they become manageable through Django's
admin panel, allowing CRUD operations directly from the admin UI.
"""

# Register your models here.
